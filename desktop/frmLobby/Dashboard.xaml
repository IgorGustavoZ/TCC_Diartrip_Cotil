<Window x:Class="WindowLobby.Dashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dashboard"
        WindowState="Maximized"
        Background="#0F172A"
        Height="1011" Width="1499"
        FontFamily="Segoe UI">

    <Window.Resources>

        <!-- Color palette -->
        <SolidColorBrush x:Key="BgBrush" Color="#0F172A"/>
        <SolidColorBrush x:Key="SidebarBrush" Color="#111827"/>
        <SolidColorBrush x:Key="CardBrush" Color="#1E293B"/>
        <SolidColorBrush x:Key="CardBrushHover" Color="#243248"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#3B82F6"/>
        <SolidColorBrush x:Key="AccentBrushHover" Color="#2563EB"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#7F1D1D"/>
        <SolidColorBrush x:Key="DangerBrushHover" Color="#991B1B"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#94A3B8"/>

        <!-- Drop shadow used on cards -->
        <DropShadowEffect x:Key="CardShadow" BlurRadius="18" ShadowDepth="2"
                           Direction="270" Color="#000000" Opacity="0.35"/>

        <!-- Sidebar nav button style -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="52"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="16,0">
                            <ContentPresenter VerticalAlignment="Center"
                                               HorizontalAlignment="Left"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E293B"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Logout button style (danger) -->
        <Style x:Key="LogoutButtonStyle" TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
            <Setter Property="Foreground" Value="#FCA5A5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="#2A1414"
                                CornerRadius="10"
                                Padding="16,0">
                            <ContentPresenter VerticalAlignment="Center"
                                               HorizontalAlignment="Left"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DangerBrushHover}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary accent button (Export PDF) -->
        <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{StaticResource AccentBrush}"
                                CornerRadius="10"
                                Padding="16,0">
                            <ContentPresenter VerticalAlignment="Center"
                                               HorizontalAlignment="Left"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrushHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Stat card style -->
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#27324A"/>
        </Style>

    </Window.Resources>

    <Grid Background="{StaticResource BgBrush}">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ============ SIDEBAR ============ -->

        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}">

            <DockPanel Margin="18,24,18,24">

                <StackPanel DockPanel.Dock="Top">

                    <StackPanel Orientation="Horizontal" Margin="6,0,0,44">
                        <Border Width="34" Height="34" CornerRadius="10" Background="{StaticResource AccentBrush}">
                            <TextBlock Text="✈" FontSize="16" Foreground="White"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="DiarTrip"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   VerticalAlignment="Center"
                                   Margin="10,0,0,0"/>
                    </StackPanel>

                    <TextBlock Text="MENU" FontSize="11" FontWeight="SemiBold"
                               Foreground="#475569" Margin="16,0,0,10"/>

                    <Button Content="👥   Usuários"
                            Style="{StaticResource NavButtonStyle}"
                            Click="BtnUsuarios_Click"/>

                    <Button Content="✈   Viagens"
                            Style="{StaticResource NavButtonStyle}"
                            Click="BtnViagens_Click"/>

                    <Button Content="⚙   Configurações"
                            Style="{StaticResource NavButtonStyle}"
                            Click="BtnConfiguracoes_Click"/>

                    <Button x:Name="BtnExpPdf" Content="📄   Exportar PDF"
                            Style="{StaticResource AccentButtonStyle}"
                            Margin="0,20,0,0"
                            Click="BtnExpPdf_Click"/>

                </StackPanel>

                <Button DockPanel.Dock="Bottom"
                        Content="🚪   Sair"
                        Style="{StaticResource LogoutButtonStyle}"
                        Click="BtnLogout_Click"/>

            </DockPanel>

        </Border>

        <!-- ============ MAIN AREA ============ -->

        <Grid Grid.Column="1" Margin="36,32,36,32">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->

            <Grid Grid.Row="0" Margin="0,0,0,28">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">

                    <TextBlock Text="Dashboard"
                               FontSize="32"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>

                    <TextBlock Text="Painel administrativo"
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="15"
                               Margin="0,4,0,0"/>

                </StackPanel>

                <Border Grid.Column="1"
                        Height="52"
                        CornerRadius="14"
                        Background="{StaticResource CardBrush}"
                        BorderBrush="#27324A"
                        BorderThickness="1"
                        Padding="14,0">

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                        <Border Width="32" Height="32"
                                CornerRadius="16"
                                BorderBrush="{StaticResource AccentBrush}"
                                BorderThickness="2">
                            <Border CornerRadius="14" ClipToBounds="True">
                                <Image x:Name="imgPerfil" Stretch="UniformToFill"/>
                            </Border>
                        </Border>

                        <TextBlock Name="txtUsuario"
                                   Text="Admin"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontWeight="SemiBold"
                                   Margin="10,0,2,0"
                                   VerticalAlignment="Center"/>

                    </StackPanel>

                </Border>

            </Grid>

            <!-- STAT CARDS -->

            <UniformGrid Grid.Row="1" Columns="3" Margin="-10,0,-10,28">

                <Border Style="{StaticResource StatCardStyle}">
                    <Grid Margin="22,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="VIAGENS" Foreground="#FF94A3B8"
                                       FontSize="12" FontWeight="SemiBold"/>
                            <TextBlock x:Name="txtViagens" Text="0"
                                       FontSize="36" FontWeight="Bold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,6,0,0"/>
                        </StackPanel>
                        <Border Grid.Column="1" Width="44" Height="44" CornerRadius="12"
                                Background="#1D3A63" VerticalAlignment="Top">
                            <TextBlock Text="✈" FontSize="18" Foreground="#60A5FA"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>

                <Border Style="{StaticResource StatCardStyle}">
                    <Grid Margin="22,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="USUÁRIOS" Foreground="{StaticResource TextMutedBrush}"
                                       FontSize="12" FontWeight="SemiBold"/>
                            <TextBlock Name="txtUsuarios" Text="0"
                                       FontSize="36" FontWeight="Bold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,6,0,0"/>
                        </StackPanel>
                        <Border Grid.Column="1" Width="44" Height="44" CornerRadius="12"
                                Background="#1D3A2E" VerticalAlignment="Top">
                            <TextBlock Text="👥" FontSize="18" Foreground="#4ADE80"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>

                <Border Style="{StaticResource StatCardStyle}">
                    <Grid Margin="22,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="CHAT IA" Foreground="{StaticResource TextMutedBrush}"
                                       FontSize="12" FontWeight="SemiBold"/>
                            <TextBlock Name="txtChatIA" Text="0"
                                       FontSize="36" FontWeight="Bold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,6,0,0"/>
                        </StackPanel>
                        <Border Grid.Column="1" Width="44" Height="44" CornerRadius="12"
                                Background="#3A2A1D" VerticalAlignment="Top">
                            <TextBlock Text="💬" FontSize="18" Foreground="#FBBF24"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>

            </UniformGrid>

            <!-- NAVIGABLE CONTENT -->

            <Border Grid.Row="2"
                    Background="{StaticResource CardBrush}"
                    BorderBrush="#27324A"
                    BorderThickness="1"
                    CornerRadius="18"
                    Padding="18"
                    Effect="{StaticResource CardShadow}">

                <Frame x:Name="MainFrame"
                       NavigationUIVisibility="Hidden"
                       Navigated="MainFrame_Navigated"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       Background="Transparent"/>

            </Border>

        </Grid>

    </Grid>

</Window>
